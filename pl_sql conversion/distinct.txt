-----------------------DISTINCT-------------
CREATE OR REPLACE PROCEDURE SELECT1(EMP_COUNT OUT NUMBER)
IS
BEGIN
SELECT COUNT(DISTINCT(DOB)) INTO EMP_COUNT FROM EMPLOYEES;
END;
SET SERVEROUTPUT ON
DECLARE
EMP_COUNT NUMBER;
BEGIN
SELECT1(EMP_COUNT);
DBMS_OUTPUT.PUT_LINE(EMP_COUNT);
END;