--------------------CASE-----------
CREATE OR REPLACE PROCEDURE SELECT1(EMP_ID NUMBER,UPDATE_NAME VARCHAR2)
IS
IS_THERE NUMBER;
BEGIN 
IS_THERE:=NULL;
SELECT DECODE(NAME,NULL,0,1) INTO IS_THERE FROM EMPLOYEES WHERE EMP_ID=ID;
CASE IS_THERE 
      when NULL then INSERT INTO EMPLOYEES (ID,NAME) VALUES(EMP_ID,UPDATE_NAME); 
      when 0 THEN UPDATE EMPLOYEES SET NAME =UPDATE_NAME WHERE ID=EMP_ID;
      else dbms_output.put_line('ALREADY ID AND NAME EXISTS'); 
END CASE; 
END;
BEGIN
SELECT1(3,'CCC');
END;
SELECT * FROM EMPLOYEES;