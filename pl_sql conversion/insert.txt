-------------INSERT------------------
CREATE OR REPLACE PROCEDURE SELECT1(EMP_ID NUMBER,UPDATE_NAME VARCHAR2)
IS
IS_THERE NUMBER;
BEGIN
IS_THERE:=NULL;
SELECT DECODE(NAME,NULL,0,1) INTO IS_THERE FROM EMPLOYEES WHERE EMP_ID=ID;
IF IS_THERE=NULL THEN
INSERT INTO EMPLOYEES (ID,NAME) VALUES(EMP_ID,UPDATE_NAME);
ELSIF IS_THERE=0 THEN
UPDATE EMPLOYEES SET NAME =UPDATE_NAME WHERE ID=EMP_ID;
END IF;
END;
BEGIN
SELECT1(6,'FFF');
END;